/**
 * @author Tarchoun Abir
 */
export interface post{
    id?:number ;
	image? : string;
    tagline? : string;
    postTranslations? : string;
    title? : string;
    description ? : string;
    content ? : string;
    slug ? : string;
    subtitle ? : string;
}