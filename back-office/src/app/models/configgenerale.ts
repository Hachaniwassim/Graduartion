export interface configgenerale{
    id?:number ;
	facebook? : string;
    twitter? : string;
    youtube? : string;
    image? : string;
    adresse ? : string;
    email ? : string;
    phone ? : string;
    fax ? : string;
    title ? : string;
    newslettertitle ? : string;
    newslettersubtitle ? : string;
    tagline ? : string;
}